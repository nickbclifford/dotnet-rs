# Safety Citation Audit — Phase 1.1

**Status:** completed 2026-08-31. This preserves the pre-edit mapping for every uncited package-owned `// SAFETY:` comment identified at the start of this phase: 223 production `src/` sites and seven bench/test sites. Each row was assigned from the comment’s local prose and the immediately justified unsafe operation, rather than from crate membership.

**Disposition vocabulary:** an existing predicate is named directly; **new predicate needed** is limited to the ISA precondition; **Plan 06 trust candidate** is limited to external P/Invoke ABI/library trust; **premise repair required** must be repaired before a citation is added.  The proposed F11 candidates are deliberately not represented by F10 raw-memory predicates.

| Site | Local SAFETY claim (verbatim) | Pre-edit mapping |
| --- | --- | --- |
| `crates/dotnet-assemblies/src/host.rs:787` | SAFETY: Access is serialized by env_lock(), preventing concurrent mutation during this test. | premise repair required — process-local `env_lock()` cannot exclude unrelated environment readers |
| `crates/dotnet-assemblies/src/host.rs:795` | SAFETY: Access is serialized by env_lock(), preventing concurrent mutation during this test. | premise repair required — process-local `env_lock()` cannot exclude unrelated environment readers |
| `crates/dotnet-assemblies/src/host.rs:800` | SAFETY: Access is serialized by env_lock(), preventing concurrent mutation during this test. | premise repair required — process-local `env_lock()` cannot exclude unrelated environment readers |
| `crates/dotnet-assemblies/src/loader.rs:548` | SAFETY: `res_ptr` was obtained from `Box::into_raw(Box::new(res))` two lines above.  Ownership is transferred to `self.metadata`; the `MetadataArena` will call `Box::from_raw` on it in its `Drop` impl.  `res_s` holds an `Arc<MetadataArena>` ensuring the arena (and thus `res_ptr`) remains live for as long as any `ResolutionS` derived from it exists. | `F9.MetadataArenaOutlivesDescriptors`; `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/resolution.rs:887` | SAFETY: aligned has `cap * 8 >= len` bytes; the u8 slice is valid for that range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-assemblies/src/resolution.rs:893` | SAFETY: We manually track this leaked box to reclaim it later. | `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/resolution.rs:895` | SAFETY: `aligned_ptr` is a live allocation just returned by `Box::into_raw`, and the arena records ownership to reclaim it without dereferencing the pointer at this call. | `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/resolution.rs:903` | SAFETY: 'aligned_slice' is valid for its entire length; it contains 'len' bytes read from the file. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-assemblies/src/resolution.rs:918` | SAFETY: We manually track this leaked box to reclaim it later. | `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/support.rs:127` | SAFETY: The source and destination are both valid for 'len' bytes. The destination buffer 'aligned' has enough capacity as it's allocated with 'div_ceil(8)'. Both buffers are non-overlapping as 'aligned' is newly allocated. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-assemblies/src/support.rs:139` | SAFETY: `aligned_ptr` came from `Box::into_raw` immediately above. AssemblyLoader owns the `Arc<MetadataArena>` that tracks and eventually reclaims the allocation, so this mutable slice remains live for every metadata user given the `'static` reference. | `F9.MetadataArenaOutlivesDescriptors`; `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/support.rs:143` | SAFETY: `aligned_ptr` is a live allocation from `Box::into_raw`; MetadataArena takes ownership of reclaiming it and does not dereference it here. | `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/support.rs:150` | SAFETY: 'aligned_slice' is a leaked Box<[u64]> which is valid for its entire length. Converting it to a *const u8 slice of length 'len' is safe because it was initialized with exactly 'len' bytes from SUPPORT_LIBRARY. | `F9.MetadataArenaOutlivesDescriptors`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-assemblies/src/support.rs:170` | SAFETY: `support_res_ptr` came from `Box::into_raw` immediately above. MetadataArena tracks and eventually reclaims it, and its owning Arc outlives every ResolutionS that receives this `'static` reference. | `F9.MetadataArenaOutlivesDescriptors`; `F10.RawAllocationOwnership` |
| `crates/dotnet-assemblies/src/support.rs:174` | SAFETY: `support_res_ptr` is the live allocation just produced by `Box::into_raw`, and MetadataArena records responsibility for reclaiming it without accessing it now. | `F10.RawAllocationOwnership` |
| `crates/dotnet-benchmarks/benches/managed_ptr_serde.rs:73` | SAFETY: `arena_handle_owner` is retained until after `arena.mutate` returns, and the transmuted reference is used only to construct the GCHandle passed within that closure. | `F1.GcHandleRooted` |
| `crates/dotnet-benchmarks/benches/managed_ptr_serde.rs:118` | SAFETY: The source was produced from a live Heap ManagedPtr and the VM cache type models the traced, collection-epoch-invalidated production owner. | `F3.InteriorPointerRebased` |
| `crates/dotnet-benchmarks/benches/managed_ptr_serde.rs:253` | SAFETY: `source` was produced by `ManagedPtr::write` from a live benchmark case and remains unchanged for the duration of this benchmark iteration; the resolver supplies the live Stack and Static bases. | `F3.InteriorPointerRebased` |
| `crates/dotnet-benchmarks/benches/metadata_load.rs:43` | SAFETY: `backing` has `cap * 8 >= len` bytes; src/dst are valid and non-overlapping. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-benchmarks/benches/metadata_load.rs:55` | SAFETY: `_backing` is valid for at least `len` bytes for the lifetime of `self`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-benchmarks/benches/runtime_primitives.rs:69` | SAFETY: the call stack is rooted in this arena and this benchmark mutates it only through this arena's `mutate_root` callback. | `F1.GcHandleRooted` |
| `crates/dotnet-cli/tests/feature_config_tests.rs:109` | SAFETY: `raw` is non-null and used only to construct the compile-time variant probe. | premise repair required — address-only `ObjectPtr::from_raw` violates its program-lifetime allocation contract |
| `crates/dotnet-exceptions/src/lib.rs:50` | SAFETY: field_bytes contains a valid ObjectRef from the object's storage. | `F2.DescriptorMatchesEcmaLayout`; `F1.GcHandleRooted` |
| `crates/dotnet-exceptions/src/lib.rs:85` | SAFETY: field_data is a valid mutable slice of the object's instance storage, and val is a valid StackValue::ObjectRef. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-delegates/src/operations.rs:204` | SAFETY: `t` is a live GC handle from the delegate target; exposing its address only derives the process-local hash component and neither dereferences nor mutates it. | `F1.GcHandleRooted` |
| `crates/dotnet-intrinsics-reflection/src/types/mod.rs:337` | SAFETY: The intrinsic decoded these managed-pointer out parameters from the CLR call frame; their origins, offsets, and scalar layouts match the values written. | `F2.DescriptorMatchesEcmaLayout`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-reflection/src/types/mod.rs:349` | SAFETY: The intrinsic decoded these managed-pointer out parameters from the CLR call frame; their origins, offsets, and scalar layouts match the values written. | `F2.DescriptorMatchesEcmaLayout`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-reflection/src/types/mod.rs:384` | SAFETY: The intrinsic decoded these managed-pointer out parameters from the CLR call frame; their origins, offsets, and scalar layouts match the values written. | `F2.DescriptorMatchesEcmaLayout`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-reflection/src/types/mod.rs:397` | SAFETY: The intrinsic decoded these managed-pointer out parameters from the CLR call frame; their origins, offsets, and scalar layouts match the values written. | `F2.DescriptorMatchesEcmaLayout`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:75` | SAFETY: `heap` borrow pins the string storage for this scope; we only use the pointer to build a managed reference with validated bounds below. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:84` | SAFETY: `heap` borrow keeps vector storage alive and we only derive an offset pointer after explicit start/length range checks. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:225` | SAFETY: `start <= total_len` and `actual_length` checks above ensure this computed element offset remains within the source span's backing allocation. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:421` | SAFETY: `length_ref` points to Span `_length` field and we write exactly 4 bytes (`i32`) to that location. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:534` | SAFETY: `inner` borrow keeps vector backing storage alive while deriving a pointer to its first element. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:753` | SAFETY: bounds checks above prove the byte adjustment stays within the source span. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:833` | SAFETY: the owning handle is retained by the ManagedPtr below, and the vector borrow keeps its backing storage live while deriving the data pointer. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:924` | SAFETY: source span metadata supplied the managed origin and base offset; the length check above and element-size stride keep this read within the source span. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/conversions.rs:946` | SAFETY: destination span metadata supplied the managed origin and base offset; its validated length is at least the source length, so this write is in bounds. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/ctor.rs:139` | SAFETY: We only take the base pointer while holding `inner` borrow. The owner handle is stored in `ManagedPtr`, so subsequent accesses are validated against this vector's lifetime and bounds. | `F10.BorrowedStorageStable`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/equality.rs:50` | SAFETY: `b_chunk` was derived from the same validated extent as `a_slice`; this helper exposes exactly `current_chunk` bytes only while computing their read-only comparison. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/equality.rs:68` | SAFETY: The chunk bounds come from `run_chunked_compare`, which limits every offset to `total_bytes`, the validated extent of both managed pointers. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/equality.rs:71` | SAFETY: As above, the validated chunk offset is within `b`'s shared extent. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/equality.rs:74` | SAFETY: `a_chunk` covers `current_chunk` validated bytes and `with_data` exposes those bytes only for the read-only comparison callback. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/equality.rs:339` | SAFETY: The bounds check above proves `index` is in the span and `element_size` is the validated layout stride used to construct `base_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:28` | SAFETY: The field layout identifies one complete ManagedPtr representation in `data`. | `F3.InteriorPointerRebased` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:64` | SAFETY: `span_ptr` points to a Span value validated by the caller and `ptr_bytes` matches the serialized `ManagedPtr` width expected for the `_reference` field. | `F3.InteriorPointerRebased` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:67` | SAFETY: The field position and serialization buffer were validated above; `read_bytes` accesses exactly that in-bounds `_reference` representation. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:81` | SAFETY: `ptr_bytes` was just read from managed memory using `ManagedPtr` serialization size, we pass the current branded GC token for lifetime validation, and `ctx` resolves live Stack and Static bases for executable pointer recovery. | `F3.InteriorPointerRebased` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:113` | SAFETY: `_length` field offset/layout come from validated span layout metadata for `span_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:143` | SAFETY: `_reference` offset/layout are resolved from the span layout, and the serialized `ManagedPtr` value matches that field's storage contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:156` | SAFETY: `_length` offset/layout are resolved from span metadata and we write a plain i32 value in the expected representation for that field. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:208` | SAFETY: We only read the vector's backing data pointer for bounds computation. `inner` borrow keeps the storage alive while this pointer is used. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-span/src/helpers.rs:254` | SAFETY: All size/offset bounds above guarantee the `[byte_offset, byte_offset + total_size)` range is valid for `m_ptr`'s origin before exposing a byte slice to `f`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/accessors.rs:80` | SAFETY: The active immutable borrow keeps the string storage alive and this branch establishes the storage variant required by `raw_data_ptr`. | `F10.BorrowedStorageStable` |
| `crates/dotnet-intrinsics-string/src/constructors.rs:210` | SAFETY: The native `String(char*)` constructor contract requires a live, null-terminated unmanaged UTF-16 address; the loop below owns that proof. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/constructors.rs:223` | SAFETY: The CLR pointer overload contract supplies a valid, null-terminated u16 string; each iteration remains within that string until the terminating zero is observed. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:11` | SAFETY: `u16` slices are contiguous; converting to an equal-size byte view is safe. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:13` | SAFETY: Same as `lhs_bytes`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:28` | SAFETY: SSE2 support is checked at runtime above. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-intrinsics-string/src/simd.rs:36` | SAFETY: NEON is baseline on aarch64. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-intrinsics-string/src/simd.rs:52` | SAFETY: SSE2 support is checked at runtime above. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-intrinsics-string/src/simd.rs:59` | SAFETY: NEON is baseline on aarch64. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-intrinsics-string/src/simd.rs:103` | SAFETY: Bounds guarantee at least 16 bytes from `offset`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:105` | SAFETY: `chunk_ptr` addresses at least 16 readable bytes in `chars`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:133` | SAFETY: Bounds guarantee at least 16 bytes from `offset`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:135` | SAFETY: `chunk_ptr` addresses at least 16 readable bytes in `chars`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:191` | SAFETY: Bounds guarantee at least 8 u16 lanes from `offset`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:193` | SAFETY: `chunk_ptr` addresses at least eight readable u16 lanes in `chars`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:198` | SAFETY: `lane_matches` is valid for 8 lanes. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:223` | SAFETY: Bounds guarantee at least 8 u16 lanes from `offset`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:225` | SAFETY: `chunk_ptr` addresses at least eight readable u16 lanes in `chars`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-string/src/simd.rs:229` | SAFETY: `lanes` is valid for 8 lanes. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:65` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches byte-width CAS. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:96` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches 16-bit CAS. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:126` | SAFETY: `target_ptr` is the managed `ref T` argument for this intrinsic, and the size/orderings match the selected primitive operation. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:153` | SAFETY: `target_ptr` is the managed `ref T` argument for this intrinsic, and the size/orderings match the selected primitive operation. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:181` | SAFETY: `target_ptr` is the managed `ref T` argument for this intrinsic, and the size/orderings match pointer-width CAS. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:223` | SAFETY: `target_ptr` is the managed `ref T` argument and `comp_raw`/`val_raw` use the same tagged object representation as regular field writes. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:246` | SAFETY: `prev_raw` came from VM-managed object slot bytes and `gc` brands the returned reference to the current arena lifetime. | `F1.GcHandleRooted` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:282` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches byte-width exchange. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:308` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches 16-bit exchange. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:333` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches `i32`. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:354` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches `i64`. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:376` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches pointer width. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:417` | SAFETY: `target_ptr` is the managed `ref T` argument and `val_raw` uses the VM tagged object representation. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:433` | SAFETY: `prev_raw` came from VM-managed object slot bytes and `gc` brands the returned reference to the current arena lifetime. | `F1.GcHandleRooted` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:470` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches `i32`. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/interlocked.rs:496` | SAFETY: `target_ptr` is the managed `ref T` argument and size matches `i64`. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/monitor.rs:156` | SAFETY: `success_ptr` was validated as writable before monitor entry and denotes the native `lockTaken` out parameter, which is one byte wide. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-threading/src/monitor.rs:249` | SAFETY: `success_ptr` was validated as writable before monitor entry and denotes the native `lockTaken` out parameter, which is one byte wide. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:55` | SAFETY: The intrinsic resolves `target_ptr` from a by-ref CLR argument and this branch enforces a 1-byte atomic width that matches the target type dispatch. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:73` | SAFETY: The pointer came from managed by-ref argument materialization and this branch performs a 2-byte atomic read consistent with the dispatch-selected type width. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:91` | SAFETY: `target_ptr` is a managed by-ref location and this dispatch arm guarantees a 4-byte atomic read for 32-bit scalar/float payloads. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:113` | SAFETY: `target_ptr` is validated by the VM and this arm uses an 8-byte atomic read, matching the resolved 64-bit payload kind. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:136` | SAFETY: Pointer-sized volatile reads use the runtime's object-reference width and the by-ref target pointer is sourced from managed stack state. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:155` | SAFETY: This path is selected only for object-reference targets and reads exactly one pointer-sized slot atomically from managed memory. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:169` | SAFETY: `val` was read atomically from an object-reference slot and `gc` brands the reconstructed handle to the current arena lifetime. | `F1.GcHandleRooted` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:195` | SAFETY: Byte and Int16 volatile write paths both convert from Int32 and perform an atomic store using the dispatch-selected integer width. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:269` | SAFETY: This branch is only for 32-bit payloads and stores exactly 4 bytes into the managed by-ref location selected by the intrinsic dispatch. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:296` | SAFETY: This arm handles only 64-bit payloads and performs an 8-byte atomic write against a managed by-ref pointer validated by the VM. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:323` | SAFETY: Pointer-sized values are written using the runtime pointer width and the destination pointer originates from a managed by-ref argument. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-threading/src/volatile.rs:356` | SAFETY: This branch stores one object-reference-sized slot atomically; ObjectRef values are serialized through `ObjectRef::write` to preserve any runtime tagging protocol. | `F10.RawMemoryAccessValid`; `F4.WidthAligned` |
| `crates/dotnet-intrinsics-unsafe/src/buffer.rs:31` | SAFETY: Unsafe buffer operations explicitly interpret NativeInt as an unmanaged address; their caller owns pointer validity. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/buffer.rs:40` | SAFETY: The managed pointer is non-null and we only read the raw address. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/buffer.rs:60` | SAFETY: Destination pointer is valid for `total_count` bytes by intrinsic contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/buffer.rs:153` | SAFETY: `ptr_info` returned the managed origin and offset for `destination`; the layout was resolved for the generic element type before the write. | `F3.InteriorPointerRebased`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/marshal.rs:17` | SAFETY: This intrinsic's caller supplies the byte offset and preserves the managed pointer's origin; `offset` only forms the corresponding managed address. | `F3.InteriorPointerRebased` |
| `crates/dotnet-intrinsics-unsafe/src/marshal.rs:23` | SAFETY: The native pointer overload delegates its range/validity contract to the CLR caller; this operation only applies that requested byte offset. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:35` | SAFETY: `start` is within the caller-validated source range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:37` | SAFETY: `start` is within the caller-validated destination range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:39` | SAFETY: The two chunk pointers are valid for `current_chunk` bytes; `copy` preserves memmove semantics for the overlapping range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:53` | SAFETY: `offset` is within the caller-validated source range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:55` | SAFETY: `offset` is within the caller-validated destination range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:57` | SAFETY: The two chunk pointers are valid for `current_chunk` bytes and may overlap. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:70` | SAFETY: `offset` is within the caller-validated destination range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:72` | SAFETY: `offset` is within the caller-validated source range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:74` | SAFETY: This branch proved the source and destination ranges do not overlap. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:94` | SAFETY: `offset` is within the caller-validated destination range. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/mem_helpers.rs:96` | SAFETY: The destination chunk is valid for `current_chunk` bytes by intrinsic contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:30` | SAFETY: `with_data(0, ...)` exposes no bytes and only obtains the address associated with this live managed pointer for the Unsafe.AsPointer contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:34` | SAFETY: `Unsafe.AsPointer` explicitly interprets NativeInt as an unmanaged address; the intrinsic caller owns pointer validity. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:526` | SAFETY: `Unsafe.AsRef(void*)` explicitly interprets NativeInt as an unmanaged address; the intrinsic caller owns pointer validity. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:536` | SAFETY: `with_data(0, ...)` only exposes the address represented by the live managed pointer; no data is read or written here. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:640` | SAFETY: `ptr_info` validates the source pointer's origin and offset, and `layout` and `target_type` were resolved for the requested generic type. | `F3.InteriorPointerRebased`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-intrinsics-unsafe/src/unsafe_ptr.rs:690` | SAFETY: `ptr_info` validates the destination pointer's origin and offset, while `layout` was resolved for the requested generic type before this write. | `F3.InteriorPointerRebased`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-metrics/src/lib.rs:1065` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1076` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1087` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1098` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1109` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1120` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1131` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1142` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-metrics/src/lib.rs:1154` | SAFETY: The guard guarantees the pointed RuntimeMetrics outlives this scope. | premise repair required — make `ActiveRuntimeMetricsGuard` lifetime-carrying before `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:39` | SAFETY: `handle` is a live GC handle supplied by the marshaling path; its object lock remains valid for `'gc`, and the resulting guard is retained by this PinnedGuard. | `F1.GcHandleRooted` |
| `crates/dotnet-pinvoke/src/call.rs:324` | SAFETY: `call_data.cif` is a prepared libffi CIF, `call_data.arg_ptrs` points to marshalling-owned argument storage valid for the duration of the call, and `ret_ptr` points to writable storage matching the return ABI (or is null for void returns). | Plan 06 trust candidate — new `F11.PInvokeAbiAgreement` (also `F10.RawMemoryAccessValid` for Rust buffers) |
| `crates/dotnet-pinvoke/src/call.rs:348` | SAFETY: `ffi_call` initialized the entire return slot after ABI validation. | Plan 06 trust candidate — new `F11.PInvokeAbiAgreement` (also `F10.RawMemoryAccessValid` for Rust buffers) |
| `crates/dotnet-pinvoke/src/call.rs:688` | SAFETY: `args` contains libffi Type values constructed for every signature parameter; their raw pointers remain valid for the duration of this marshaling loop. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:741` | SAFETY: The unmanaged pointer's validity contract is carried by StackValue; this helper only keeps the pointer-valued argument storage alive for the call. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:777` | SAFETY: `h` is pinned above for the full call duration, so the underlying object storage address remains stable while libffi uses this pointer. | `F1.GcHandleRooted`; `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:780` | SAFETY: The pinned guard guarantees stable backing storage and a valid data pointer for the lifetime of `guard`. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:788` | SAFETY: Non-null object storage is pinned and its guard remains in `local_guards`; null is a valid pointer argument value. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:808` | SAFETY: The heap owner is pinned above, and this guard is retained in `local_guards` for the whole synchronous native call. | `F1.GcHandleRooted`; `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:811` | SAFETY: `guard` holds the object storage lock until it is retained below. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:813` | SAFETY: The typed-reference slot's offset was established for this live object origin, whose storage remains pinned and guarded through the call. | `F10.BorrowedStorageStable`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:829` | SAFETY: `ptr` is an owned cross-arena lock handle retained by the slot. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:832` | SAFETY: `guard` keeps the cross-arena object storage live and immobile until it is retained in `cross_arena_guards` below. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:835` | SAFETY: The typed-reference slot's offset was established for this live cross-arena object origin and remains valid while `guard` is retained. | `F10.BorrowedStorageStable`; `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:842` | SAFETY: The slot retains the managed pointer's live origin. For stack, static, transient, and unmanaged origins, their established P/Invoke lifetime contract keeps the resolved pointer valid through this call. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:879` | SAFETY: `p.with_data` validates access against the managed origin and we copy at most `buf_len` bytes into an owned buffer of the same length. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:917` | SAFETY: Heap pointers are pinned and their guards are kept alive in `local_guards`; non-heap pointers come from `ManagedPtr` origin data and are forwarded as-is for the duration of this call. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:932` | SAFETY: A handle-less Heap origin reaches this branch only as the P/Invoke raw-address fallback; native-call validity remains the caller's contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:942` | SAFETY: Heap storage is pinned and its guard remains in `local_guards`; non-heap ManagedPtr origins retain their backing storage through the call. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call.rs:967` | SAFETY: `ptr` is an owned non-null handle to the cross-arena lock cell created by the VM; taking a shared reference here does not outlive the handle. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:971` | SAFETY: The borrow guard keeps the object storage alive and immobile while this pointer is used by libffi. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call.rs:975` | SAFETY: `cross_arena_guards` retains the borrow that keeps this object storage alive and immobile through the FFI call. | `F10.BorrowedStorageStable` |
| `crates/dotnet-pinvoke/src/call_types.rs:149` | SAFETY: `layout` is validated above. A null return is handled as OOM. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call_types.rs:169` | SAFETY: `ptr` points to `layout.size()` bytes for this allocation; callers only read up to `len`, which is <= `layout.size()`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call_types.rs:177` | SAFETY: `ptr` was allocated with `alloc_zeroed` using `layout` in `new_zeroed`. | `F10.RawAllocationOwnership` |
| `crates/dotnet-pinvoke/src/call_types.rs:205` | SAFETY: `Cif` owns a prepared `ffi_cif`; rtype is set by `ffi_prep_cif`. | Plan 06 trust candidate — new `F11.PInvokeAbiAgreement` |
| `crates/dotnet-pinvoke/src/call_types.rs:215` | SAFETY: null checked above. | Plan 06 trust candidate — new `F11.PInvokeAbiAgreement` |
| `crates/dotnet-pinvoke/src/call_types.rs:286` | SAFETY: `dest_ptr` originates from validated argument marshalling for this call and we copy exactly `len` bytes from an owned temporary buffer. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/call_types.rs:303` | SAFETY: `origin` and `offset` were captured from validated managed pointers and `data` references owned temporary storage. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-pinvoke/src/loader.rs:141` | SAFETY: Loading a dynamic library is inherently unsafe because constructors may run and symbol layouts are unchecked. The sandbox gate and curated name list constrain inputs to approved libraries, and failures are surfaced as `PInvokeError::LoadError`. | Plan 06 trust candidate — new `F11.NativeLibraryLoadTrusted` |
| `crates/dotnet-pinvoke/src/loader.rs:184` | SAFETY: We request the raw symbol as an untyped C function pointer and immediately pass it to libffi. Arity/signature validation is handled by metadata-driven marshalling before invocation; lookup failure is converted to `PInvokeError::SymbolNotFound`. | Plan 06 trust candidate — new `F11.PInvokeAbiAgreement` |
| `crates/dotnet-runtime-resolver/src/factory.rs:359` | SAFETY: Scalar-to-ValuePointer conversion is the CLI unmanaged-address boundary; no managed origin exists. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-runtime-resolver/src/factory.rs:545` | SAFETY: `data.len() >= ManagedPtr::SIZE` is checked above, and the bytes come from VM-managed storage that uses ManagedPtr's serialization format. | `F3.InteriorPointerRebased`; `F6.NoEscapeAcrossArena` |
| `crates/dotnet-runtime-resolver/src/factory.rs:561` | SAFETY: This legacy `ValuePointer` representation is a raw-address storage boundary. `data` contains no owner or origin handle from which provenance could be recovered, so its producer must ensure the address remains valid for the resulting unmanaged pointer's use. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-runtime-resolver/src/factory.rs:566` | SAFETY: The preceding raw-storage boundary contract supplies the unmanaged pointer validity premise. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-runtime-resolver/src/factory.rs:580` | SAFETY: These reference-like CLR types are serialized as `ObjectRef` and `data` is provided by managed storage with the expected object-reference width. | `F2.DescriptorMatchesEcmaLayout`; `F1.GcHandleRooted` |
| `crates/dotnet-runtime-resolver/src/factory.rs:588` | SAFETY: Class-typed values are represented as `ObjectRef` payloads in storage and use the same branded deserialization contract as other reference types. | `F2.DescriptorMatchesEcmaLayout`; `F1.GcHandleRooted` |
| `crates/dotnet-runtime-resolver/src/factory.rs:601` | SAFETY: Non-value `Type` instances are runtime object references serialized as `ObjectRef` bytes in managed storage. | `F2.DescriptorMatchesEcmaLayout`; `F1.GcHandleRooted` |
| `crates/dotnet-runtime-resolver/src/types.rs:267` | SAFETY: Cross-arena object pointers are live GC handles maintained by the thread manager; borrowing their lock yields the stable shared object view. | `F1.GcHandleRooted`; `F10.BorrowedStorageStable` |
| `crates/dotnet-simd/src/aarch64.rs:12` | SAFETY: NEON is part of the baseline ISA on aarch64. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/aarch64.rs:20` | SAFETY: The loop bounds guarantee at least 16 bytes remain for both slices. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:22` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `left_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:24` | SAFETY: The loop bounds guarantee at least 16 bytes remain for both slices. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:26` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `right_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:40` | SAFETY: NEON is part of the baseline ISA on aarch64. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/aarch64.rs:50` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:52` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `src_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:54` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:56` | SAFETY: The loop bounds guarantee at least 16 writable bytes at `dst_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:62` | SAFETY: Tail copy stays in-bounds and does not overlap by contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:64` | SAFETY: Tail copy stays in-bounds and does not overlap by contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:66` | SAFETY: Both tail pointers are valid for `len - offset` bytes and the source and destination slices do not overlap by this function's contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:74` | SAFETY: NEON is part of the baseline ISA on aarch64. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/aarch64.rs:85` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/aarch64.rs:87` | SAFETY: The loop bounds guarantee at least 16 writable bytes at `dst_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:123` | SAFETY: The caller guarantees pointer validity and non-overlap for `len` bytes. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:125` | SAFETY: The caller guarantees pointer validity for `len` bytes. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:160` | SAFETY: The caller guarantees pointer validity for `len` bytes. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:177` | SAFETY: Same safety contract as `fill_raw`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:217` | SAFETY: Arrays are valid for 16 bytes and do not overlap. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:221` | SAFETY: `dst` is valid for all writes below. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/lib.rs:225` | SAFETY: `dst` is valid for all writes below. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:31` | SAFETY: SSE2 support is guaranteed on x86_64 and checked at runtime on x86. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/x86.rs:39` | SAFETY: The loop bounds guarantee at least 16 bytes remain for both slices. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:41` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `left_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:43` | SAFETY: The loop bounds guarantee at least 16 bytes remain for both slices. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:45` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `right_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:63` | SAFETY: SSE2 support is guaranteed on x86_64 and checked at runtime on x86. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/x86.rs:73` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:75` | SAFETY: The loop bounds guarantee at least 16 readable bytes at `src_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:77` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:79` | SAFETY: The loop bounds guarantee at least 16 writable bytes at `dst_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:85` | SAFETY: Tail copy stays in-bounds and does not overlap by contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:87` | SAFETY: Tail copy stays in-bounds and does not overlap by contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:89` | SAFETY: Both tail pointers are valid for `len - offset` bytes and the source and destination slices do not overlap by this function's contract. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:101` | SAFETY: SSE2 support is guaranteed on x86_64 and checked at runtime on x86. | new predicate needed — `F10.ArchIntrinsicPrecondition` |
| `crates/dotnet-simd/src/x86.rs:112` | SAFETY: The loop bounds guarantee at least 16 bytes remain. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-simd/src/x86.rs:114` | SAFETY: The loop bounds guarantee at least 16 writable bytes at `dst_ptr`. | `F10.RawMemoryAccessValid` |
| `crates/dotnet-types/src/comparer.rs:1549` | SAFETY: pointer comes from Box::into_raw; MetadataArena::drop calls Box::from_raw on the same pointer, so ownership is correctly transferred. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/comparer.rs:1553` | SAFETY: `ptr` was just created with `Box::into_raw`; `arena` owns it and converts it back exactly once in `MetadataArena::drop`. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/comparer.rs:1592` | SAFETY: `ptr` was just created with `Box::into_raw`; `arena` owns it and converts it back exactly once in `MetadataArena::drop`. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/generics.rs:845` | SAFETY: `ptr` was just created with `Box::into_raw`; `arena` owns it and converts it back exactly once in `MetadataArena::drop`. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/lib.rs:42` | SAFETY: `dotnetdll`'s `basic_index!` macro currently defines `TypeIndex` as `pub struct TypeIndex(pub(crate) usize)`. Its sole field accepts every `usize` bit pattern, so `index` is valid for `TypeIndex`; the const assertion above confirms that the two types currently have equal sizes. `TypeIndex` intentionally lacks a public constructor: dotnetdll's checked constructors (e.g. `Resolution::type_definition_index`) bounds-check against a live `Resolution`, which by design has no counterpart here — this helper only ever produces the compile-time null sentinel below, where no `Resolution` exists to check against and the resulting index is never used to index one (see `TypeDescription::is_null`). This transmute is the permanent approach for that sentinel, not a stopgap. | `F2.DescriptorMatchesEcmaLayout` — assumed upstream representation; Plan 06 tension (source calls it permanent) |
| `crates/dotnet-types/src/resolution.rs:59` | SAFETY: The pointer was added via add_resolution, which requires it to be from Box::into_raw. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/resolution.rs:65` | SAFETY: The pointer was added via add_u64_slice, which requires it to be from Box::into_raw. | `F10.RawAllocationOwnership` |
| `crates/dotnet-types/src/resolution.rs:73` | SAFETY: MetadataArena contains raw pointers to metadata that are owned exclusively. The metadata is immutable after construction. Access to the storage is guarded by Mutex. The raw pointers themselves point to 'static data (leaked boxes), and the Arc ensures the arena lives as long as any descriptor pointing into it. | `F7.InitializationPublished`; `F9.MetadataArenaOutlivesDescriptors` |
| `crates/dotnet-types/src/resolution.rs:78` | SAFETY: See the immediately preceding `Send` proof; synchronized access and immutable metadata also make shared references to this arena safe. | `F7.InitializationPublished`; `F9.MetadataArenaOutlivesDescriptors` |
| `crates/dotnet-types/src/resolution.rs:95` | SAFETY: ResolutionS carries an Arc<MetadataArena> alongside a raw pointer. The Arc ensures the metadata arena (and thus all metadata) lives as long as any ResolutionS exists. The raw pointer lifetime is guaranteed by the Arc. Thread-safety is guaranteed by the Arc's ref-counting and the read-only nature of metadata once loaded. | `F7.InitializationPublished`; `F9.MetadataArenaOutlivesDescriptors` |
| `crates/dotnet-types/src/resolution.rs:101` | SAFETY: See the immediately preceding `Send` proof; the shared metadata is immutable and the owning `Arc<MetadataArena>` remains synchronized. | `F7.InitializationPublished`; `F9.MetadataArenaOutlivesDescriptors` |
| `crates/dotnet-types/src/resolution.rs:125` | SAFETY: The pointer in ResolutionS is kept alive by the Arc<MetadataArena> that is co-carried in the same Option variant. The arena owns the metadata and will not drop it until all ResolutionS instances are dropped. | `F9.MetadataArenaOutlivesDescriptors` |
| `crates/dotnet-vm-data/src/lib.rs:104` | SAFETY: MethodInfo<'static> contains only 'static metadata borrows, ordinary values, and Arc-owned metadata; it contains no GC handles that need tracing. | `F5.TracesEveryGcRef` |
| `crates/dotnet-vm-data/src/stack.rs:119` | SAFETY: `StackFrame` traces all GC-managed fields it can contain: `exception_stack` entries and optional `multicast_state`. `frame_continuation` carries only `RuntimeType` metadata (`'static`) and has no GC references. | `F5.TracesEveryGcRef` |
| `crates/dotnet-vm-data/src/stack.rs:138` | SAFETY: `EvaluationStack` traces all GC-managed fields (`stack` and `suspended_stack`). Scratch pointer caches are raw addresses derived from stack values and contain no GC references. | `F5.TracesEveryGcRef` |
| `crates/dotnet-vm-data/src/stack.rs:227` | SAFETY: `slot_ptr` identifies the start of the live stack slot and `off` was recorded from that slot when the pointer was created. | `F3.InteriorPointerRebased` |
| `crates/dotnet-vm-data/src/stack.rs:230` | SAFETY: adding the recorded offset to a non-null stack-slot pointer preserves non-nullness. | `F3.InteriorPointerRebased` |
| `crates/dotnet-vm-data/src/stack.rs:251` | SAFETY: `slice` is exactly one serialized ManagedPtr field, selected by the layout manager from storage it previously wrote. | `F3.InteriorPointerRebased` |
