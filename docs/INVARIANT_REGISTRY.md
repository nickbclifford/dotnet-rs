# Invariant Registry

This is the authoritative index of named predicates cited by `// SAFETY:`
comments.  A citation identifies the invariant; the co-located prose explains
why it holds at that site.  `scripts/check_doc_drift.sh` verifies that every
source citation names a row below and that every row has a source citation.

| Name | Family | Statement | What establishes it | Defined in | Falsifier |
| --- | --- | --- | --- | --- | --- |
| `F1.StwParked` | Rooting / STW liveness | A GC-only access occurs only while every mutator that could access the storage is parked. | `StopTheWorldGuard` / `GcCycleGuard` protocol | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | STW multithreaded tests and guard-off CI leg |
| `F1.ArenaGenerationMatch` | Rooting / STW liveness | A cross-arena pointer is dereferenced only while its live lease has the recorded arena generation. | `ArenaLease`, `active_leases`, and generation comparison | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | `unregister_arena`/re-register tests |
| `F1.GcHandleRooted` | Rooting / STW liveness | A GC reference used through raw storage remains rooted for the duration of the access. | `Gc<'gc>` branding, lock guards, or an active arena lease | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | Miri and GC stress tests |
| `F2.DescriptorMatchesEcmaLayout` | Layout faithfulness | A descriptor's offsets, sizes, and field types match the instantiated ECMA-335 layout it describes. | nothing — assumed | [Architecture — Invariants](ARCHITECTURE.md#invariants) | Layout differential fixtures |
| `F3.StackSlotMatchesView` | Eval-stack slot typing | A raw evaluation-stack view is read only as the type with which that slot was pushed. | stack-value discriminants and instruction stack discipline | [Architecture — Invariants](ARCHITECTURE.md#invariants) | stack/conversion tests |
| `F3.InteriorPointerRebased` | Eval-stack slot typing | Every recorded interior pointer is associated with its originating slot and is re-established after stack reallocation. | pointer-origin bookkeeping and `apply_reallocation_fixup()` | [Architecture — Invariants](ARCHITECTURE.md#invariants) | managed-pointer round-trip and offset fuzzers |
| `F4.WidthAligned` | Atomic width / alignment | An atomic access has valid storage aligned for its selected width and has no conflicting non-atomic access. | alignment checks and synchronization/exclusion guards | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | atomic alignment tests |
| `F5.TracesEveryGcRef` | Tracing completeness | Each `Collect::trace` implementation visits every GC reference contained in its value. | exhaustive field/variant traversal and `Collect` implementations | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | GC retention tests |
| `F6.NoEscapeAcrossArena` | `gc-arena` brand discipline | A value branded with an arena lifetime cannot escape that arena or be used under another arena's brand. | invariant lifetimes, private constructors, and `for<'gc>` confinement | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | cross-arena tests; the deliberate `Gc::from_ptr` exception is separately justified |
| `F7.InitializationPublished` | Immutable-field publication | A lock-free immutable-field read observes an initializing write that happened-before publication. | nothing — assumed | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | publication concurrency tests |
| `F8.LockOrderRespected` | Lock order & safepoint discipline | Locks are acquired only in the declared DAG order. | `define_lock_order_dag!` and negative `AcquireAfter` assertions | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | compile-time lock-order assertions |
| `F8.NoSafepointWhileHeapBorrowed` | Lock order & safepoint discipline | Code holding a heap borrow neither polls a safepoint nor allocates until that borrow is released. | `GcScopeGuard` conventions and safepoint-exclusion guards | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | GC-scope protocol tests |
| `F9.MetadataArenaOutlivesDescriptors` | Leaked-`Box` metadata lifetime | `MetadataArena` exclusively owns leaked metadata allocations and outlives every descriptor referring to them. | conventional owner-tied `Arc<MetadataArena>` reasoning | [GC and Memory Safety — Invariants](GC_AND_MEMORY_SAFETY.md#invariants) | descriptor ownership tests |
| `F10.RawMemoryAccessValid` | Raw-memory access contracts | A raw-memory read, write, copy, or pointer derivation stays within live storage of the required size, initialization, alignment, and aliasing mode. | a preceding bounds/integrity check and guard, or the immediate unsafe function's documented caller contract | [Architecture — Invariants](ARCHITECTURE.md#invariants) | raw-memory bounds, aliasing, and Miri tests |
| `F10.RawAllocationOwnership` | Raw-allocation ownership | A raw allocation is converted to a reference or reclaimed only with the provenance, lifetime, and unique ownership supplied by its originating allocation protocol. | `Box::into_raw`/`Box::leak` paired with the single matching conversion or documented raw-pointer contract | [Architecture — Invariants](ARCHITECTURE.md#invariants) | Miri ownership tests |
| `F10.BorrowedStorageStable` | Raw-memory access contracts | A pointer obtained from managed or shared backing storage is used only while the borrow or lock that stabilizes that storage remains active. | `Gc`/object borrow, `with_data(_mut)` closure, or the relevant read/write guard | [Architecture — Invariants](ARCHITECTURE.md#invariants) | GC-scope and concurrent-storage tests |
| `F11.CliLoadKindMatchesStorage` | CLI scalar representation | A raw atomic scalar is decoded only through the `LoadType` whose bit-width and signedness describe that storage. | exhaustive `LoadType` dispatch; object-handle branch relies on its documented caller contract | [Architecture — Invariants](ARCHITECTURE.md#invariants) | CTS/CLI conversion tests |
| `F11.ObjectPointerThreadSafe` | Object-pointer concurrency | `ObjectPtr` is sent or shared between threads only because its pointee is a `ThreadSafeLock<ObjectInner>` with the required `Send`/`Sync` bounds. | `ThreadSafeLock` implementation and compile-time `assert_impl_all!` checks | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | `ObjectPtr` Send/Sync assertions |
| `F11.PInvokeLastErrorSerialized` | P/Invoke host state | The process-global P/Invoke last-error slot is read or written only by callers that serialize access to it. | the unsafe host-operation contract | [Threading and Synchronization — Invariants](THREADING_AND_SYNCHRONIZATION.md#invariants) | P/Invoke concurrency tests |
