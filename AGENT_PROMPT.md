You are executing step **{{STEP_ID}}** ("{{STEP_TITLE}}") of the dotnet-rs architecture refactor plan.

## Project context

- **Repo root:** `/home/nick/Desktop/dotnet-rs`
- **What it is:** A Rust implementation of the .NET Common Language Infrastructure (ECMA-335 VES). ~63k lines across 27 crates under `crates/`. Core VM is `dotnet-vm`; entry point is `dotnet-cli`.
- **CI:** `check.sh` is the canonical local CI script. It runs clippy (`-D warnings`) and tests for every feature combination from `xtask matrix test-features`, plus lock-order harnesses and hang-probe integration tests. Any change must keep `check.sh` green under every feature combination.
- **Build-time codegen:** Instruction dispatch and intrinsic PHF tables are generated by `crates/dotnet-vm/build.rs`. Read `docs/BUILD_TIME_CODE_GENERATION.md` before touching anything related to instructions or intrinsics.

## Your assignment

Execute **only** step {{STEP_ID}}. Do not start adjacent steps even if they look related or easy. If you discover that the current step depends on something not yet done, stop, document it, and do not proceed.

### Step description

{{STEP_DESCRIPTION}}

### Context from the review

{{REVIEW_SECTION}}

## Mandatory procedure

1. **Read prior memory** — read `AGENT_MEMORY.md` in full before doing anything else. Prior agents may have left warnings, partial states, or discoveries that affect your work.
2. **Verify current state** — before making any change, verify that the code at the cited file:line still matches what REVIEW.md describes. Code may have changed since the review was written.
3. **Make the change** — implement the step as described. Keep changes minimal; do not fix adjacent issues, even if tempting.
4. **Run verification:**
   - For small/isolated changes: `cargo clippy --all-targets --no-default-features --features multithreading -- -D warnings && cargo test --no-default-features --features multithreading -- --nocapture`
   - For changes touching `dotnet-vm-ops`, `dotnet-vm`, or multiple crates: run `bash check.sh` (can take 10–20 min)
   - For pure documentation changes: no build required, but verify no broken Markdown links.
5. **Tick the box** in `CHECKLIST.md` (change `- [ ]` to `- [x]` for step {{STEP_ID}}).
6. **Append a memory entry** to `AGENT_MEMORY.md` using the format defined there.
7. **Do not expand scope** — if you discover additional work, add new checklist items at the end of the relevant phase in `CHECKLIST.md` and note them in your memory entry. Do not silently do extra work within this step.
8. **Surface concerns** — if something in the review plan turns out to be wrong, dangerous, or more complex than expected, document it clearly in your memory entry and do not proceed. Ask for owner guidance.

## What to report back

When you are done, write a brief summary:
- What you changed (files touched, lines added/removed)
- Whether `check.sh` (or the narrower check) passed
- Any surprises or new findings
- Any new checklist items you added

## Prior memory

{{PRIOR_MEMORY}}

## Current checklist state

{{CHECKLIST_SNAPSHOT}}
